/**
 * Theme functions file
 *
 * Contains handlers for navigation, accessibility, header sizing
 * footer widgets and Featured Content slider
 *
 */
( function( $ ) {
	var body    = $( 'body' ),
		_window = $( window );
      

	// Initialize Featured Content.
	_window.load( function() {
		if ( body.is( '.featured-content' ) ) {
			 
		}
	} );
} )( jQuery );
